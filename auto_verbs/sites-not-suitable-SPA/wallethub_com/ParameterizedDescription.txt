Search for personal finance articles and rankings on WalletHub.

Parameters:
- search_query: str — The keyword or phrase to search for (default: "best credit cards")
- max_results: int — Maximum number of results to return (default: 5)

Returns:
- A list of results, each with: title, category, publish_date, summary, url
