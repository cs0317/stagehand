// Auto-generated – Tom's Hardware – Hardware Review Search
const { setupLLMClient, PlaywrightRecorder } = require("../../stagehand-utils");
const { genPython } = require("../../stagehand-utils");

async function tomshardware_search(page, request) {
  const query = request?.query || "best gaming motherboard 2024";
  // Uses Google site search: site:tomshardware.com + query
  // See Python implementation for full logic
}

module.exports = { tomshardware_search };
