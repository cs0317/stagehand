# Searches Athlinks for race results matching a race name. For each result,
# extracts runner name, bib number, finish time, overall place, age group,
# and age group place.

@dataclass(frozen=True)
class AthlinksRequest:
    race_name: str = "Boston Marathon"
    max_results: int = 5

@dataclass
class RaceResult:
    runner_name: str = ""
    bib_number: str = ""
    finish_time: str = ""
    overall_place: str = ""
    age_group: str = ""
    age_group_place: str = ""

@dataclass
class AthlinksResult:
    results: list = field(default_factory=list)

def athlinks_search(page: Page, request: AthlinksRequest) -> AthlinksResult:
