# Searches Uber for available ride options between a pickup and dropoff location, returning up to max_results estimates.

@dataclass(frozen=True)
class UberRideSearchRequest:
    pickup: str
    dropoff: str
    max_results: int

@dataclass(frozen=True)
class UberRideEstimate:
    ride_type: str
    price_range: str

@dataclass(frozen=True)
class UberRideSearchResult:
    pickup: str
    dropoff: str
    estimates: list[UberRideEstimate]

def search_uber_rides(page: Page, request: UberRideSearchRequest) -> UberRideSearchResult
